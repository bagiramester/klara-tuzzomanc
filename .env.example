# Környezeti változók sablon — másold .env néven és töltsd ki
# A .env sosem kerül a repóba (lásd .gitignore)

# Node környezet: development | production
NODE_ENV=development

# Szerver port (alapértelmezés: 5000)
PORT=5000
